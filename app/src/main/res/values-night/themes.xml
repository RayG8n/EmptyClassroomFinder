<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.EmptyClassroomFinder" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- In dark mode, we lean more into the "Chalkboard" look -->
        <item name="android:windowBackground">@color/chalkboard_green</item>
        <item name="colorPrimary">@color/chalk_white</item>
        <item name="android:fontFamily">monospace</item>
    </style>

    <style name="Theme.EmptyClassroomFinder" parent="Base.Theme.EmptyClassroomFinder" />

    <!-- Minimalist Button Style for Dark Mode -->
    <style name="Widget.Classroom.Button" parent="Widget.Material3.Button">
        <item name="android:backgroundTint">@color/chalk_white</item>
        <item name="android:textColor">@color/chalkboard_green</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="shapeAppearance">@null</item>
    </style>
</resources>